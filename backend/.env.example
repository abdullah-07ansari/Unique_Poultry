# MongoDB Connection URI
# In development, if a local server is not running on 27017, the application will automatically
# fall back to a MongoMemoryServer instance.
MONGODB_URI=mongodb://127.0.0.1:27017/poultrysense

# Server Listening Port
PORT=5000

# Environment Mode (development / production)
NODE_ENV=development
